module Composition where

-- Is a type of High Order Functions that allows to combine functions s.t. the result of applying one function gets passed to the next function as an argument

-- Application to the args should happen after we composed the 2 functions.